<H3>Books</H3>

<table>
  <thred>
  	<tr>
  		<th></th>
  		<th>Title</th>
  		<th>Opinion</th>
  	</tr>
  </thred>
  <tbody>
    <% @books.each do |book| %>
  	<tr>
	  	<td></td>
	  	<td><%= link_to book.title,book_path(blog) %></td>
	  	<td><%= book.opinion %></td>
    </tr>
    <% end %>
    <%= pagenate @book,class: "paginate" %>
  </tbody>
</table>